# PLAN

## Information Gathered
- Repo currently contains only `README.md`.
- `README.md` includes a full **TECHNOBIOFASCIAL CODEX — Agent Handoff Document** describing a Claude.ai sandbox React artifact (`codex-v8.jsx`).

## Plan
1. Create a clean starter folder structure aligned to the Codex React artifact (docs, app, assets, scripts).
2. Add minimal placeholder files (so repo has a usable baseline):
   - `docs/README.md`
   - `docs/codex-v8-notes.md`
   - `app/README.md`
   - `scripts/validate-jsx.mjs`
   - `package.json` with a JSX validator dependency (`@babel/parser`).
3. Update top-level `README.md` with a short pointer to the codex artifact workflow.
4. (Optional follow-up) If a `codex-v8.jsx` file is added later, wire it into the scripts and validate it.

## Dependent Files to be edited
- `/workspaces/symmetrical-goggles/README.md`

## Followup steps
- Run `npm install` and `npm run validate:jsx` once `codex-v8.jsx` exists.


