# codex-v8 Notes

This repo’s `README.md` includes the full **TECHNOBIOFASCIAL CODEX — Agent Handoff Document**.

Key workflow:
- The canonical artifact is `codex-v8.jsx` (single-file React artifact).
- Validate JSX before saving via the provided script(s) once dependencies are installed.
- Upload/copy the artifact into the Claude sandbox working directory.


